# 更改记录 2026-08-13

## 批次三（V1 D5+D6）：新生地层——沉降、出露、记忆双读

### 为什么改（玩家视角）

- **旧痕迹不再凭空消失，而是沉进土里**：以前土堆太多时最老的会被"剪掉"。
  现在它会先塌成一小片旧迹（还能点，语气是"它们住过这里"），再慢慢沉成
  地表上一点几乎看不出的土色异样——存在过的东西只是沉下去了，没有被删掉。
  塌洞也从出生那天起就踏上这条路：永远留下，但会一年年往下沉。
- **沉下去的速度看世界怎么过日子**：风大的年份沙埋得快，雨多的年份泥盖得快，
  草盛的地方根缠得快。埋得深不深，本身就是那几年的记录。
- **沉底的东西偶尔会被翻出来**：风暴夜（激烈情绪之后）可能把旧物剥出地表；
  田鼠在附近打新洞，也可能把隔壁的旧东西翻上来。重见天日的那件会半埋在原地，
  当天会有一个注意标记指给你看——旧物回来了，这是新闻。
- **同一件旧迹，见证过它和没见证过的人，读到的不是同一句话**：
  它发生时你在场（在线看到，或后来读信知道了它），点它时说的是
  "这堆土你见过的——镇子还在的时候。如今沉下去半截了"；
  它属于你错过的年代，说的就是"一处旧迹。比你早"。
  每句都带着它入土时的年代感——那年代由世界翻过几页来定，不由时钟来定。

### 改动清单

| 文件 | 改动 |
|---|---|
| `Assets/GlimmerDiary/Scripts/Data/WorldEntityData.cs` | 新增 `StratumRecord`（sourceKey/kind/zone/buriedDateKey/chapterOrdinal/chapterAtBurial/depth/exposed 三件套/witnessed；`RelicMaxDepth=1.0`、`MaxPerZone=8` 常量）；`StateChangeRecord`/`PermanentTerrainRecord`/`VoleTrailRecord` 加 `witnessed` |
| `Assets/GlimmerDiary/Scripts/Data/WorldStateData.cs` | `WorldSaveData` 新增 `strata`（旧档 null 零迁移，StratumSystem 构造 ??= 兜底）；`WorldChronicleEntry` 新增 `witnessKeys`（信件点名的痕迹键） |
| `Assets/GlimmerDiary/Scripts/Data/AnimalStateData.cs` | `WorldEvent` 加 `witnessed`（旧档默认 false=考古语气，语义恰好正确） |
| `Assets/GlimmerDiary/Scripts/Data/TraceKeyUtil.cs` | `MoundKeepCount=6` 常量化（binder 裁剪与地层入土共用）；`MoundRecord` 扩 trigger/toValue/witnessed；新增 `EnumVisibleMounds`（最新 N 窗口唯一实现） |
| `Assets/GlimmerDiary/Scripts/Core/StratumSystem.cs` | **新增**：`strata` 单写者，每 SimulatePass 恰 Tick 一次（VoleTownSystem 之后）。①入土：土堆出窗/小径淡完/塌洞出生；②沉降：`depth += BaseRate×(1+风×1.2+雨×0.8+草×0.6)`，只增不减，出露即冻结；③出露：风暴夜（E_env.A>0.7）8%、田鼠同区打洞 15%，确定性掷签 `Fnv1a(键\|日期\|钩子)`；封闭层断代=回放 ChapterTurned 事件（零新增字段）；`LayerPhrase` 章节→断代句 |
| `Assets/GlimmerDiary/Scripts/WorldManager.cs` | SimulatePass 挂 `_stratumSystem.Tick`；WorldTick 非 catch-up 拍前后 `WitnessSnapshot`/`WitnessStampNew`（在场打戳，集中单点，未来新系统零接入）；`MarkWitnessed`（读信回执：塌洞源记录+地层记录同步翻真）；`TryGetStratumContext` 透传（L3 不导入 Core 纪律）；`CollectWindowCollapses` 出参补身份键 → Compose 传 `collapseWitnessKeys` |
| `Assets/GlimmerDiary/Scripts/Core/AbsenceLetterComposer.cs` | `Compose` 新增 `collapseWitnessKeys` → 落条目 `witnessKeys` |
| `Assets/Script/ChronicleLetter.cs` | 读信时 `WorldManager.MarkWitnessed(save, e.witnessKeys)`（展示层簿记，经 WorldManager 静态入口写入） |
| `Assets/Script/WorldTraceBinder.cs` | T1 改走 `EnumVisibleMounds` 单一来源（种子保持去前缀键，土堆位置零漂移）；T2 塌洞移交地层分支（防同拍双画）；新增 strata 渲染三态：遗存（塌矮/色沉/微陷，可点）、地层（depth≥RelicMaxDepth 未出露：一点土色异样，**不可点**，每区预算 MaxPerZone=8，记录永不删）、出露（半埋挺回，可点，当日上新鲜标记）；新类型 `Relic`/`ExposedRelic`；签名纳入 strata 计数与 exposed 标志；顺带清理死代码（birthDays 字典与 TraceInstance.birthDay 字段，只写不读） |
| `Assets/Script/TraceCaptionBank.cs` | 新增 `relic_mem`/`relic_arch`/`exposed_mem`/`exposed_arch` 四语料组（占位各 1~2 条，设计者并行线扩写）；`Pick` 增加 `witnessed`/`layerPhrase` 参数：relic/exposed 按见证标记选语域，`{layer}` 占位统一替换（缺省回退"很久以前"） |
| `Assets/Script/TraceCaptionUI.cs` | relic/exposed 点击语经 `WorldManager.TryGetStratumContext` 取 witnessed 档+断代句注入 Pick |
| `Assets/GlimmerDiary/Scripts/Editor/AnimalDriveSmokeTest.cs` | BuildPipeline 挂 `StratumSystem`（镜像 WorldManager 序）并暴露 env；新增 `RunStrata`（出窗入土/沉降累计/过档/塌洞出生即入土+断代锚/翻页后封闭层 5 断言）、`RunExposure`（风暴必中必不中/深度门/田鼠翻土区域门/出露冻结 6 断言，掷签测试内自算期望值）、`RunWitnessed`（打戳只戳新生/读信回执双翻真/witnessKeys 落信/双语域/占位回退 5 断言） |
| `Assets/Editor/GlimmerBuild.cs` | RunSmoke 注册 Strata、Exposure、Witnessed（20 项） |
| `Assembly-CSharp.csproj` | 手补 StratumSystem.cs（离线编译用） |

### 所有权表新增

| 字段 | 写者 | 读者 |
|---|---|---|
| `WorldSaveData.strata` | StratumSystem（唯一） | WorldTraceBinder（派生渲染）、WorldManager.MarkWitnessed（witnessed 翻真）、测试 |
| 各记录 `witnessed` | WorldManager（WitnessStampNew 在场打戳 / MarkWitnessed 读信回执，唯二入口） | TraceCaptionUI（经 TryGetStratumContext 选语域）、TraceKeyUtil.MoundRecord（入土继承） |
| `WorldChronicleEntry.witnessKeys` | AbsenceLetterComposer（成信时落） | ChronicleLetter（读信时回放给 MarkWitnessed） |
| `{layer}` 文本占位 | StratumSystem.LayerPhrase（章节→断代句唯一判定） | TraceCaptionBank.Pick（经 TraceCaptionUI←WorldManager 透传） |

### 设计红线复核

- 记录只增不删（不可逆原则③）：渲染预算阀只影响画不画，不影响记录在不在。
- 出露掷签确定性（Fnv1a 哈希，不用 Random）：同一存档同一天同一结果，重启/回放可复现。
- witnessed 不暴露任何数值 UI——它只改变一句话的说法（验收三问的"误读空间"保留：
  读信知道了 ≠ 真的在场，但信这么说了，就算数）。
- 塌洞方案 A（2026-08-12 拍板）：出生即入土缓慢沉降，"永久"体现在记录永不删。

### 验证

- 离线编译：`dotnet build` Assembly-CSharp + Assembly-CSharp-Editor 均 0 错误（2026-08-13）。
- batchmode 冒烟 20 项（smoke_b3.log）：断言 FAIL=1，即既有已知 QCOrganic，无回归。
  - Strata 5 断言全绿：第 7 堆出窗入土/10 天沉降 0.07→0.74/过 RelicMaxDepth 入地层档/塌洞出生即入土+断代锚荒年/翻页后封闭层第 1 层+"雨季正盛的时候"。
  - Exposure 6 断言全绿：风暴必中签出露/必不中按兵不动/深度门（未沉底无资格）/田鼠翻土同区出露/区域门（隔壁区不动）/出露后深度冻结。
  - Witnessed 5 断言全绿：打戳只戳本拍新生/读信回执源记录+地层记录同步翻真/witnessKeys 随信落档/双语域分句+断代注入/缺省回退不留占位符。
- 首轮冒烟抓到两处实施缺陷，均为本批新增代码自身问题，已修：
  ① `TraceCaptionBank` 新语料组收尾符误写成 `],`（数组初始化误闭字典项），编译期即拦下；
  ② Witnessed 测试给 Compose 喂了 salience 0 的噪音条目导致不成信、断言空转——
  换成永久事件档条目后转绿（同批次二教训：断言要对准设计语义，不只对准 API）。

---

## 点击失灵根因修复（批次二挂号项，D9 前置清除）

### 为什么改（玩家视角）

- **点世界的点击全部恢复了**：点草地草会簌动，点痕迹/亮斑会推近出观察语。
  之前点哪都没反应，不是点击坏了，是"别点穿 UI"的保护闸门认错了人——
  它把整片草原当成了一块 UI 面板，替你"挡住"了每一次点击。

### 根因（实证链）

1. 主相机上挂着一个场景模板遗迹 `PhysicsRaycaster`（PlantGrowthTemplate.unity:12114，
   事件掩码=全层），全项目无任何消费者——但没有 EventSystem 时它完全无害，躺了很多周。
2. 08-03 demo 收口（768ba20）运行时自建 EventSystem + InputSystemUIInputModule（日记 UI 需要），
   `TraceInput` 的闸门① `IsPointerOverGameObject()` 从此刻起才真正生效
   （此前 `EventSystem.current == null` 恒短路 false——这就是"之前一直正常"的原因；
   08-03~08-12 三批验证全走键盘快进+批处理冒烟，无人点击，故迟至 08-12 暴露）。
3. 新输入模块把"指针射线打到的第一个物体"直接记为 `pointerEnter`（InputSystemUIInputModule.cs:528），
   **不过滤是否 UI**；`IsPointerOverGameObject()` 只查 `pointerEnter != null`（同文件:322）。
   PhysicsRaycaster mask=全层 → 悬停地形/痕迹恒有命中 → 闸门①吞掉一切世界点击。
4. 唯一能过闸门的点击是点天空（无碰撞体）→ 随后真的什么都没打到，
   报"射线未命中任何碰撞体"。两个症状，一个病。

### 改动清单

| 文件 | 改动 |
|---|---|
| `Assets/Script/TraceInput.cs` | 闸门①改为 `PointerOverUGUI`：`EventSystem.RaycastAll` 后只认 `GraphicRaycaster` 命中（当前项目无任何 GraphicRaycaster → 恒 false，信/日记防穿透由闸门② IsOpen 兜底；未来接入可点击 uGUI 时自动生效）。排查日志按 9194b03 约定全部清除 |

### 验证

- 离线编译 0 错误；play 实测（2026-08-13 用户确认）：点草簌动 ✓、点痕迹/亮斑推近+语料 ✓、
  信开着点世界不穿透、关信恢复 ✓。

### 遗留雷（已挂号，未动）

- 主相机 PhysicsRaycaster 仍在场景里（mask=全层）。本次修复后它不再影响点击，
  但任何未来的 `IsPointerOverGameObject` 调用都会踩同一个坑——建议择机在 Inspector 移除并存场景
  （场景手工组件删除，未由 Claude 代劳）。

---

## 批次四（V1 D7+D8）：侧翼——慢变量骨架 + 河/路/风三通道

### 为什么改（玩家视角）

- **世界比你看到的大**：舞台之外的两端——西边的草原/上游、东边的高地/下游——
  现在有自己的天气和族群日子。它们永远不可抵达、没有地图、看不到实体，
  但那边发生的事会顺着三条通道漏进你这一页：
- **河**：西边的雨季夜里下雨，水一两天后才流到你这里——
  你这里一滴雨没下，河水却涨了。信会告诉你"水是从西边来的"。
- **路**：西缘有时出现一串你没见过的脚印，一天比一天走得更深，十二天后淡掉；
  你的动物离开时，东缘会留下一串往山里去、几星期才淡完的印子；
  偶尔有连夜赶路的过路客，脚印两三天横穿整个舞台，不停留。
- **风**：西边大旱时，你能看见地平线挂上一层尘霾——晴天也挂着。
  你还没读信，不知道它意味着什么；一个月后信里说"西边的雨季没有来"，你才想起来那片霾。
- **侧翼的消息永远迟到**：水迟一两天，信迟一个月。信息以动物的速度旅行，所以世界显得辽阔。

### 改动清单

| 文件 | 改动 |
|---|---|
| `Assets/GlimmerDiary/Scripts/Data/WorldStateData.cs` | `WorldSaveData` 新增 `wings`（旧档 null 零迁移）；新增 `WingState`（west/east 慢变量 + upstreamRains 延迟队列 + traces 痕迹表）、`WingSideState`、`UpstreamRainRecord`、`WingTraceRecord`（含可见窗/蔓延段数/横穿进度静态量——放 Data 层同 FadeDays 先例，L2/L3 单源） |
| `Assets/GlimmerDiary/Scripts/Data/AnimalStateData.cs` | `WorldEventType` 新增 `WingUpstreamRain`（payload=下雨那夜 dateKey）、`StrangerPassed`（payload=stranger/passerby） |
| `Assets/GlimmerDiary/Scripts/Core/WingSystem.cs` | **新增**：`wings` 单写者，每 SimulatePass 恰 Tick 一次（StratumSystem 之后，读当日事件终态）。①慢变量周粒度漂移：每周一个 FNV 确定性目标、每日走近 1/7；②河通道：西翼越湿夜雨越勤（3%~18%/夜），雨量 0.3~0.8、延迟 1~2 日入队；抵达日发 WorldEvent+编年史；③路通道：陌生脚印（族群压力 4%~16%/日，同时最多一串）、AnimalDeparted→离去标记（幂等）、过路客（3%/日，同时最多一位）；④风通道只持有数据。全部掷签 `Fnv1a(钩子|日期)` 确定性 |
| `Assets/GlimmerDiary/Scripts/WorldManager.cs` | SimulatePass 挂 `_wingSystem.Tick`（每日最后）；`PropagateRainfallToLocations` 消费 upstreamRains 队列（riverbank×0.5、lowland×0.1 漫流）——**waterLevel 单写者纪律不破，侧翼不直写水位**；`GetWingDust01()` 透传（L3 不导入 Core 纪律） |
| `Assets/GlimmerDiary/Scripts/Core/AbsenceLetterComposer.cs` | `event_WingUpstreamRain`/`event_StrangerPassed` 入 salience 2（迁移/到离场档）——信是侧翼的主要解释渠道 |
| `Assets/Script/WorldTraceBinder.cs` | 路通道三类渲染：陌生脚印（西缘锚点起逐日蔓延，段数口径读 Data 层静态量）、离去标记（highland_east 区缘向东一串，东方位由锚点连线数据驱动、不假设坐标轴）、过路客链（五锚点横穿线按进度逐日截断）；出现/上路当日上新鲜标记；签名纳入 traces 计数；新类型 StrangerMarks/DepartureMarks/PasserbyChain |
| `Assets/Script/TraceCaptionBank.cs` | 新增 `stranger`/`departure`/`passerby` 语料组（方向句占位各 2 条，设计者并行线扩写） |
| `Assets/Script/EmotionWeatherController.cs` | 新增 `dustHaze` 外部驱动字段（默认 0 无影响）：满霾时雾距收拢至 ~60%、雾色向尘土黄褐混入（随天空亮度缩放——夜里不发光，黄昏最显） |
| `Assets/Script/WorldAtmosphereBinder.cs` | 尘霾通路：`GetWingDust01()` → 独立慢平滑（0.08，霾是"挂上去的"不是"飘过来的"）→ `weatherController.dustHaze` |
| `Assets/GlimmerDiary/Scripts/Editor/AnimalDriveSmokeTest.cs` | BuildPipeline 挂 `WingSystem`（镜像 WorldManager 序）；新增 `RunWings`（漂移确定性/有界/夜雨入队/1-2日延迟/无雨而水涨/事件+方向句 7 断言）、`RunWingTraces`（成形/蔓延/淡出留档/离去幂等/横穿进度/3日即走/方向句 8 断言）；`FindCleanRollDate` 帮手（必中且窗口内无更早触发——防唯一活跃位被抢先） |
| `Assets/Editor/GlimmerBuild.cs` | RunSmoke 注册 Wings、WingTraces（22 项） |
| `Assembly-CSharp.csproj` | 手补 WingSystem.cs（离线编译用） |

### 所有权表新增

| 字段 | 写者 | 读者 |
|---|---|---|
| `WorldSaveData.wings`（含慢变量/upstreamRains/traces） | WingSystem（唯一） | WorldManager.PropagateRainfallToLocations（来水消费）、WorldTraceBinder（痕迹渲染）、WorldManager.GetWingDust01 透传 → WorldAtmosphereBinder、测试 |
| `loc.waterLevel`（上游来水部分） | 仍是 PropagateRainfallToLocations（唯一） | 不变——侧翼只放队列记录，入账只在水位单写者内 |
| `EmotionWeatherController.dustHaze` | WorldAtmosphereBinder（唯一，LateUpdate） | EmotionWeatherController 雾公式内部消费 |

### 设计红线复核

- **侧翼永远只是侧翼**：无实体、无个体模拟、无地图；玩家能看到的三样东西
  （水涨、脚印、尘霾）全是"那边漏进来的痕迹"，无一物可前往（§5 红线）。
- 侧翼天气不读日记情绪——它有自己的日子（宪法①：世界有自己的生命）。
- 一切掷签确定性（Fnv1a 哈希，不用 Random）：同一存档同一天同一片侧翼。
- 纪元接口预留未接：`WingSystem.WestDrought01` 等静态查询已就绪，
  "西旱"章节等设计者拍板后再接纪元钟触发条件。

### 验证

- 离线编译：`dotnet build` Assembly-CSharp + Assembly-CSharp-Editor 均 0 错误（2026-08-13）。
- batchmode 冒烟 22 项（smoke_b4.log，第三跑）：断言 FAIL=1，即既有已知 QCOrganic，
  无回归——既有 17 项未被翼通道干扰（含 SoilMoisture 精确值断言：上游来水未顶偏）。
  - Wings 7 断言全绿：慢变量漂移确定性（双管线同档同日同值）/有界/必中日夜雨入队/
    1~2 日延迟抵达/无雨而水涨（riverbank 增量 >0.05）/WingUpstreamRain 事件 payload=下雨夜/
    编年史含"西边"。
  - WingTraces 9 断言全绿：陌生脚印成形当日 1 段/2 日后 3 段逐日蔓延/12 日淡完不可见/
    记录仍在档/离场→东缘标记幂等（同键不重复建）/过路客进度 0→0.5→1/3 日穿完即走/
    方向句语料（来路西、去处东）。
- 首轮冒烟抓到一处测试自身缺陷（生产代码无恙），两轮修复：
  ① 痕迹断言用 `Find(kind)` 首匹配，被洁净窗**之前**的更早有机触发抢走对象——
  改按身份键取（`stranger|{date}`/`passer|{date}`，与离去标记同手法）；
  ② 过路客选日器从世界起点扫，而①②阶段已把时钟推过该日——
  `FindCleanRollDate` 加 `from` 参数从当前时钟次日起扫。
  教训同批次三：确定性掷签下"必中日"只是入场券，还要防窗口外的有机触发污染取数路径。
- play 快进观测点（新档 Y1-M9-D1 起，确定性掷签可预告）：第 9 天过路客上路
  （Y1-M9-D9，脚印两日横穿）；第 13 天上游夜雨（Y1-M9-D13，水 14~15 日到、
  河岸无雨自涨）；第 10 月 15 日陌生脚印（Y1-M10-D15，逐日蔓延）。

---

## 设计变更：撤悬浮小球，改语料引导（留意句）

### 为什么改（玩家视角）

- **世界上不再有小球指着你说"看这里"**。新鲜痕迹头顶的悬浮亮点撤掉了——
  它是"UI 在告诉你世界变了"，而现在改成"世界在告诉你它变了"。
- **想知道今天哪里变了，就坐下写点什么**：打开日记，或写着写着停笔几秒，
  日记本顶缘会浮出一行斜体小字——"石头那边有一堆新翻的土""西边的草里有串
  你没见过的脚印"。它只说**哪个方向**，不说在哪个坐标。
- **找不到就让它过去**：不写日记的日子，世界安安静静，什么也不会弹出来。
  错过那场风就错过那些痕迹——几天后或一个月后的信里，会补上后文。
  错过也是世界的一部分。
- **看东西的方式也变了**：点一下草，草只是簌簌地晃——镜头不动。
  想走近看，就**双击**——草、石头、水面，双击哪里，镜头就过去陪它待一会儿，
  直到你点一下旁边才回老位子。痕迹例外：它单击就迎上来，还会轻轻说它那句老话——
  而且现在说得更从容些。点痕迹不用瞄准，点它附近那一带就行。

### 设计拍板（2026-08-13，设计者）

- 挂载面 = 只在日记边缘（开日记时 + 停笔几秒时浮现；不设任何世界视角出口——
  底部每日一行/常驻角落行均偏通知，否决）。
- 指方向 = 地名式（河边/低洼的草里/台地中央/石头那边/东边的高地，
  用现成区名，天然是区域不是坐标）。
- 撤球不留开关/复活口。
- **交互模型修订（同拍板，v2 定稿）**：单击草=只晃草（一个动作一个结果，不拉相机）；
  **双击任何物体**（草/石头/水面）= 聚焦该处并**停留**（不再数秒自动回主机位）；
  痕迹保持单击即聚焦+观察语（它不晃草，没有冲突），点痕迹附近 4m 内**吸附**到痕迹上
  （撤球后命中体小的解法——"点的是那一带，不是那个像素"）；
  聚焦中单击旁边/天空回主机位（回归等一个双击窗口再执行，双击转焦不会被误吞）。
  语料停留整体调长（留意句 12 秒、观察语 5.5 秒）。

### 改动清单

| 文件 | 改动 |
|---|---|
| `Assets/Script/WorldTraceBinder.cs` | 拆除整个悬浮标记系统：`SpawnFreshMarker`（运行时球体）、`_markers` 呼吸动画循环、4 个 marker 序列化字段（场景 YAML 残留无害，下次存盘自消）。**新鲜判定口径原样保留**（fresh 集合，1.5 有效日/天气系数/T7 放宽 3 日）——产物从"头顶的球"改为 `_notices` 留意清单；新增 `GetFreshNotices()` 只读查询与 `NearestZoneId`（位置→最近锚点区名）。TraceClickable/点击推近不动 |
| `Assets/Script/TraceCaptionBank.cs` | 新增 `PlacePhrase(zoneId)` 地名表（五区名+"不远处"回退）；新增 `notice_*` 语料组十类+通用 `notice` 回退组（占位各 1~2 条，设计者并行线扩写）；新增 `PickNotice`（`{place}` 注入，侧翼三类自带方向不套地名；同键稳定选句） |
| `Assets/Script/DiaryInputUI.cs` | 面板顶缘外新增留意行（15pt 斜体、恒半透明、raycastTarget=false）：打开日记拉 `GetFreshNotices()`，1.2s 后缓显第一行；停笔 5s 浮出下一行（轮换），一动笔即散；清单为空则始终安静；关日记即散，不落任何已读记录。每行停留 12s（拍板调长） |
| `Assets/Script/CameraPusher.cs` | **停留式聚焦**：`PushTo` 聚焦后不再自动回主机位（删 holdDuration 计时）；新增 `IsFocused`/`FocusPoint`/`ReturnToStage()`；转焦从当前位置平滑过去（不再先回主机位再出发） |
| `Assets/Script/TraceInput.cs` | **单击/双击分工（v2 拍板）**：单击痕迹（含 4m 吸附）聚焦+观察语；单击草只晃草不拉相机；双击任何物体聚焦该处；聚焦中单击旁边/天空回主机位——回归延迟一个双击窗口（0.3s）执行，双击转焦不被误吞。闸门①②不动 |
| `Assets/Script/TraceCaptionUI.cs` | 观察语停留 3.2s→5.5s（聚焦停留式后，语料陪着多看一会儿） |
| `Assets/GlimmerDiary/Scripts/Editor/AnimalDriveSmokeTest.cs` | 新增 `RunNoticingCaptions` 5 断言（十类语料齐备无占位残留/地名注入/地名表覆盖+回退/稳定选句/未覆盖类型通用回退） |
| `Assets/Editor/GlimmerBuild.cs` | RunSmoke 注册 NoticingCaptions（23 项） |
| `CLAUDE.md` | Interaction stance 段：悬浮标记许可改写为留意句拍板（同日设计决定同步） |

### 设计红线复核

- 留意句纯 L3 派生：binder 只读查询 + 静态语料，不写世界状态、不进 pendingChronicles、
  不落档、无已读/未读/计数——它不是收件箱，也不是通知。
- "错过"链路完整：不写日记→看不到留意句→痕迹自然老化→后文由既有信件通道
  （BehaviorNarrator/WingSystem/EraSystem 的 pendingChronicles）补上。
- 语料仍只描述所见、永不暴露数值（同 TraceCaptionBank 小报体纪律）。

### 验证

- 离线编译：`dotnet build` Assembly-CSharp + Assembly-CSharp-Editor 均 0 错误（2026-08-13）。
- batchmode 冒烟 23 项（smoke_b5.log）：断言 FAIL=1，即既有已知 QCOrganic，无回归。
  - NoticingCaptions 5 断言全绿：十类留意语齐备无占位残留/地名注入（石头那边 vs 河边）/
    地名表五区覆盖+回退/同键稳定选句/未覆盖类型通用回退仍带地名。
- play 实测（2026-08-13 用户现场确认后迭代两轮）：单击晃草不拉相机、双击聚焦停留、
  痕迹单击聚焦+语料、聚焦中点旁边回主机位、双击转焦不误吞。

---

## 三处逻辑修订：键盘隔离 / 调试快进按钮 / 音乐跟日照

### 为什么改（玩家视角）

- **写日记敲字母 l，信不会再拍上来了**——以前 L 键随时呼出信件，写到一半
  一个 l 就把信弹在脸上。
- **屏幕右下角多了一枚"推进一天"小按钮**（仅编辑器里有，随按随走一天）——
  测试世界长程变化不用再开菜单。
- **音乐换班跟着天亮天黑走，不跟钟走**——日出日落是一段坡：天蒙蒙亮时
  晨曲慢慢起，天黑了夜曲慢慢来。它看的是太阳（你眼睛看到的那个），
  不是任何时间表；手动拖光照调试时间，音乐也跟着眼前的天色走。

### 改动清单

| 文件 | 改动 |
|---|---|
| `Assets/Script/ChronicleLetter.cs` | L 键呼信加闸门：`DiaryInputUI.IsOpen` 时不触发——日记开着，l 是字母不是命令 |
| `Assets/Script/DemoUIBootstrap.cs` | 新增调试按钮（`#if UNITY_EDITOR`，不进发布构建）：右下角"推进一天 ▶"，可反复按；口径与 `Debug/Fast Forward 1 Day` 完全相同（非 catch-up 逐日模拟保世界志 + 落盘锚定）；画布带 GraphicRaycaster（按钮可点 + TraceInput 闸门①认得它，点按钮不推相机）。常驻提示行文案同步新交互："J 写日记 · 双击走近 · L 读信" |
| `Assets/ImportedAssets/PleebieJeebies/Scripts/LightManager.cs` | 新增 `CurrentSunlight01`（太阳仰角正弦，地平线以下=0）——UpdateLighting 每次求值时单点赋值；外部驱动/内部时钟/手动调试拖时间都反映在内，读者得到的是"此刻眼睛看到的日照"（不含 weatherDim 天气压光） |
| `Assets/Script/AmbientAudio.cs` | 昼夜 BGM 白天度改读 `LightManager.CurrentSunlight01`（原读世界节律 dayProgress 的 0.25/0.75 固定过渡带，整段 band 逻辑与 musicBandHalfWidth 字段删除）；惰性查找 LightManager；音乐装载补 `Audio/day`/`Audio/Days`/`Audio/night`/`Audio/Nights` 回退（用户素材文件名，同 Grass 先例） |

### 所有权表新增

| 字段 | 写者 | 读者 |
|---|---|---|
| `LightManager.CurrentSunlight01` | LightManager.UpdateLighting（唯一） | AmbientAudio（昼夜 BGM 白天度） |

### 验证

- 离线编译：`dotnet build` Assembly-CSharp 0 错误（2026-08-13）。
- 三处均为 L3/UI 层，batchmode 冒烟不覆盖（23 项无回归风险面）；play 验证项：
  日记里敲 l 不弹信、按钮连按日期逐日走且世界志逐封到、拖光照调试时间音乐随天色换班。



---

## 聚焦体验修订：滚轮俯仰 / 小径聚焦取链上最近段

### 为什么改（玩家视角）

- **走近之后可以滚动滚轮抬头低头了**——双击凑近一件痕迹后，滚轮把视线从
  平视（与目标齐平，最低不低过平行）一路抬到正头顶俯视（90°封顶），
  绕着你盯的那一处看，距离不变。
- **点河岸不再被拽去镇子中间看田鼠了**——田鼠镇的小径是一条长链，以前
  点在链上任何一处，镜头都飞到整条链的正中心（恰是土堆集中的镇心）。
  现在点哪一段，就凑近看哪一段。

### 改动清单

| 文件 | 改动 |
|---|---|
| `Assets/Script/CameraPusher.cs` | 新增聚焦中滚轮俯仰：停稳后（运镜协程结束）读滚轮，绕 FocusPoint 保距抬压仰角并持续注视；钳 [pitchMinDeg=0°, pitchMaxDeg=90°]；指数平滑（pitchSmooth=12）；落位低于平行先抬回平行起步；信/日记开着不抢滚轮。新参 pitchPerNotchDeg=10（一格 10°） |
| `Assets/Script/TraceClickable.cs` | 新增 `focusChain`（链式痕迹沿路采样点）与 `NearestFocus(at)`：普通痕迹返回 focusPoint，链式痕迹返回折线上离 at 最近的投影点 |
| `Assets/Script/WorldTraceBinder.cs` | `TraceInstance` 增 `chainPoints`；`SpawnVoleTrail` 收集实际铺下的每片色片位置入链；`FinishTrace` 把链转给 `TraceClickable.focusChain` |
| `Assets/Script/TraceInput.cs` | `FocusTrace` 带点击落点，聚焦点改取 `NearestFocus(clickAt)`；`NearestTrace` 吸附距离也改按"链上最近点"算——长链不再靠盒心抢吸附 |

### 验证

- 离线编译：`dotnet build` Assembly-CSharp 0 错误（2026-08-13）。
- 纯 L3 输入/展示层，batchmode 冒烟不覆盖；play 验证项：双击痕迹后滚轮
  平视↔正俯视往返不越界、点河岸小径东头看东头/西头看西头、点旁边仍回主机位。

---

## 聚焦体验修订·补：滚速 4 倍 / "就近聚焦"推广到所有长条痕迹

### 为什么改（玩家视角）

- **滚轮俯仰变跟手了**——上一版一格才 10°，从平视滚到正俯视要滚八九格；
  现在一格 40°，两三格到头。
- **点河岸还是飞中间——这次抓到了真凶**：河岸边的长条痕迹不止田鼠小径，
  脚印串、过路客链、气味标记全都是"一条长链挂一个大命中盒，注视点取盒心"，
  上一轮只修了小径一种。现在所有长条痕迹（跨度超 4 米）一律就近聚焦；
  另外大盒子的顶面会先把点击射线截在半空，就近参照改用你手指实际指的
  地面落点，不再用盒面上的截停点。

### 改动清单

| 文件 | 改动 |
|---|---|
| `Assets/Script/CameraPusher.cs` | `pitchPerNotchDeg` 10 → 40 |
| `Assets/Script/WorldTraceBinder.cs` | `TraceInstance` 增 `propPositions`（AddProp 逐件记录落点）；`FinishTrace` 就近聚焦从"仅小径"推广到一切 XZ 跨度 > 4m 的多件痕迹（脚印串/过路客链/气味标记/离人痕等全部覆盖） |
| `Assets/Script/TraceInput.cs` | 分支①就近参照点改用 `hitPoint`（地形实际落点），`traceHitPoint`（盒顶面截停点）仅兜底 |

### 验证

- 离线编译：`dotnet build` Assembly-CSharp 0 错误（2026-08-13）。
- play 验证项：滚轮两三格平视到顶；点河岸脚印串的任意一段聚焦该段；
  点小径/过路链同样就近。

---

## 痕迹可发现性改造·第一批（不含田鼠小灯）

### 为什么改（玩家视角）

- **田鼠窝不再只是一小堆土**：新土外先露出被扒开的一圈地面，走近能看见土堆侧面的黑洞口；塌洞则是更宽的沉暗裸土圈、塌陷唇和黑色洞心，两者不会再混成同一种小土斑。
- **田鼠小径从散点变成路**：踩踏色片间距缩短并沿路排齐，舞台机位能先看见连续窄带；留意句明确说它连接的是几处洞口。
- **旱裂先有可找的地面，再有裂缝细节**：草海里出现浅色白泥滩 / 土斑，近看才读到放大的主裂与支裂。
- **“石头旁边”变成唯一地标**：石区与东高地边界固定出现一块裂脸石。狐狸擦痕留在朝舞台的一面，鹿鼠脚印走到石脚停住；留意句直接说“裂脸石”，不再让玩家猜 419 块散石中的哪一块。
- 田鼠小灯本批明确不做，等待技术方案与整体夜景语言另行拍板。

### 改动清单

| 文件 | 改动 |
|---|---|
| `Assets/Script/WorldTraceBinder.cs` | 只读派生层改造六类痕迹：新窝加裸土圈与洞口；塌洞加沉暗外圈、放大塌陷唇和洞心；田鼠小径改连续窄带；旱裂加浅色裸土斑、4～5 条放大裂缝与低强度压草；狐狸记号与鹿鼠停步改为围绕稳定“裂脸石”布置。裂脸石运行时确定性生成，不改 scene/prefab、不写存档。 |
| `Assets/Script/TraceCaptionBank.cs` | 新增 marks/rangehalt/cracks/vtrail 专属留意语；土堆与塌洞留意语改写为可见特征；`stone_area` 指向“裂脸石一带”，旱裂按 lowland/center 分别说“白泥滩/浅色土斑”。 |
| `Assets/GlimmerDiary/Scripts/Editor/AnimalDriveSmokeTest.cs` | NoticingCaptions 覆盖从 10 类扩到 14 类，新增唯一地标与关系词断言。 |
| `Docs/交互设计工作台_2026-07-21_矩阵补全.md` | 回填第一批六类痕迹的远看信号、近看细节和留意语锚点；记录小灯延期。 |

### 所有权与边界

| 对象 | 写者 | 读者 / 说明 |
|---|---|---|
| 痕迹触发、寿命、世界记录 | 原有 L2 单写者不变 | 本批不新增、不改写任何数据字段或记录 |
| 痕迹 mesh、颜色、压草与裂脸石 | `WorldTraceBinder`（L3 展示层） | 只读 `WorldSaveData` / `ZoneMap`；不调用模拟入口、不落盘 |
| 留意句 | `TraceCaptionBank` 静态语料 | `DiaryInputUI` 只读展示；无已读状态、计数或通知压力 |

### 验证

- `dotnet build Assembly-CSharp.csproj --no-restore`：0 警告，0 错误。
- `dotnet build Assembly-CSharp-Editor.csproj --no-restore`：0 警告，0 错误。
- Unity 刷新 / 域重载完成，Console 编译错误与警告均为 0。
- `GlimmerDiary/Test Noticing Captions`：7/7 PASS；14 类留意语齐备，裂脸石、白泥滩和“洞口之间的路”关系断言全绿。
- 固定舞台截图验证在切换播放模式时遇到 Unity MCP 域重载断线，未把未确认截图写成通过项；需恢复编辑器连接后补拍。代码与语料验证结果不受影响。

---

## 田鼠灯（Vole Lamp）：小径附属造物，夜间引导+照明

### 为什么改（玩家视角）

- **夜里的小径边亮起了一点一点的暖光**：田鼠镇成形后，小径沿线会插起细杆，
  杆顶一粒珠。黄昏太阳贴地平线的几分钟里，它们一盏一盏慢慢亮起来；黎明再慢慢熄下去。
  远处看是一点暖光，走近了灯下的草被照出一小圈琥珀色的光斑。
- **黑夜里"石头那边"终于有东西可看了**：留意句说"入夜路边有灯亮着"——它仍只给方向，
  但那一点光就是方向的落点。看见了想走近，点一下小径，镜头就去陪那段路待一会儿。
- **镇散了，灯就不亮了**：田鼠镇衰落的那天起，夜里再没有人点灯——杆和珠还立在原地，
  只是暗着，和小径一起慢慢长回草里。灯灭是镇散的第一个信号。
- **白天它们只是不起眼的小杆子**：暗褐色的小造物插在路边，不发光、不抢眼。
- **语料不点破**：留意句说"入夜路边有灯亮着"，点击说"天黑了它们就亮。谁擦的，没人登记。"——
  杆是谁立的、珠是什么，世界不作任何说明。

### 改动清单

| 文件 | 改动 |
|---|---|
| `Assets/Script/TraceKit.cs` | 新增 `VoleLampPost`（细高双锥灯杆，顶端小弯头，原点在底端）与 `VoleLampBead`（小八面体灯珠，原点在珠心）程序化网格 + `VoleLampBeadAnchor` 挂点常量 |
| `Assets/Script/WorldTraceBinder.cs` | `SpawnVoleTrail` 末尾 `SpawnVoleLamps`：种子 `Fnv1a("vlamp|{formedDateKey}")` 从链点按 ≥8m 间距抽灯位（上限 12 盏/镇，确定性可复现）；每盏=灯杆 prop+灯珠 prop+子 GameObject 挂 Point Light（暖色 (1.0,0.58,0.25)、range 4m、intensity 1.5、`shadows=None`——省阴影贴图且豁免 LightManager 的 LampPreset 染色，恒暖"人造火"色）；新增公开只读 `ActiveVoleLamps`（Rebuild 开头清空、随 SpawnVoleTrail 重注册，灯随痕迹 root 销毁无悬挂引用）；lapsed 镇杆珠照插但不注册——镇散灯灭，造物随小径淡出；灯珠材质运行时懒创建（URP/Lit+Emission，零场景布线，同 FallbackMaterial 口径）；Awake 自动挂 VoleLampDriver |
| `Assets/Script/VoleLampDriver.cs` | **新增**（L3）：每帧读 `LightManager.CurrentSunlight01`（+weatherDim 暴雨夜压暗，与风暴藏星同逻辑），日照 0.10→0 间 SmoothStep 爬升——黄昏渐亮/黎明渐灭全程连续无跳变；呼吸 ±8% 周期约 4s 相位错开（稳定油灯式，不做飞舞/断闪）；新灯 2 秒淡入；驱动 Light.intensity 与灯珠 MPB `_EmissionColor`（HDR ×2.5 吃 Bloom）/`_BaseColor`（白天压暗褐） |
| `Assets/Script/TraceCaptionBank.cs` | `notice_vtrail` 升级为"路+灯"双线 2 条（"入夜路边有灯亮着"）；`vtrail` 点击组补灯句 1 条——含灯不坐实"谁点的灯" |
| `Docs/交互设计工作台_2026-07-21_矩阵补全.md` | 小灯"待拍板"记录更新为已拍板并实施（定位/技术/昼夜行为/引导衔接四组拍板口径） |
| `Assembly-CSharp.csproj` | 手补 VoleLampDriver.cs（离线编译用） |

### 机制互联声明（宪法原则④）

读：`WorldSaveData.voleTrails`（经 binder 派生）、`LightManager.CurrentSunlight01`/`weatherDim`。
对它做出反应的已有系统：`Signature()` 重建（voleTrails 计数/lapsed/gameDay 已在签名内，灯生灭自动触发）、
`TraceClickable` 聚焦链（灯挂在小径 trace 下自动获得链上就近聚焦）、Bloom 后处理（灯珠 HDR>1 泛光）。
无任何系统需要"知道灯的存在"；L2 零改动、零存档迁移。

### 设计红线复核

- 灯是造物痕迹不是动物（无痕原则⑤）：不做萤火虫式飞舞/断闪，稳定油灯式呼吸。
- 夜里的光是世界内造物（有生命周期、镇散即灭），不是悬浮 UI 标记——
  与留意句同一信息粒度（方向级）的另一感官通道，不违背"只给方向不给位置"。
- 质感复刻现有光照通路：真照明走 `GlimmerToonCore._ADDITIONAL_LIGHTS`（与闪电灯同一路，自动过 toon 色阶），
  发光观感走 HDR>1 吃 Bloom（阈值 1.0）；灯珠不走自发光 shader 旁路。
- 布点确定性（Fnv1a+链几何，不用 Random 全局态）：读档/重建/重启后灯位逐点复现。
- 间距 ≥8m = 2×range：地面任一点最多 1-2 盏在范围内，稳在 URP 每物体附加光上限 4 之内。

### 验证

- 离线编译：`dotnet build Assembly-CSharp.csproj` 0 错误（2026-08-13；5 个警告均为历史遗留）。
- play 验证项（待用户现场确认）：
  1. Fast Forward 至 `[VoleTown] 小径成形` → 拖时间入夜：黄昏灯珠+照明渐亮无跳变、
     灯下草出现 toon 色阶暖光斑、远机位读作一点暖光带 Bloom；拖回正午灯珠暗褐、intensity=0。
  2. 推进至镇散 → 当夜灯不再亮；20 日内杆珠随小径淡出。
  3. 存档退出重进：灯位逐点复现。
  4. 夜里点击小径任一段聚焦最近链点；开日记确认 notice_vtrail 双线句出现。

### 原在灯（同日追加拍板）：镇成形之前就在的老灯

- **镇子还没影的时候，夜里就不是全黑的了**：裂脸石旁、河边、台地中央、东边高地口——
  这四盏灯世界诞生之初就在，比镇老，不知道谁立的。镇散它们也不灭。
- **一眼可辨的新旧**：原在灯是冷的——月白色、更暗、呼吸更深更慢（约 10 秒一轮，
  像风里的老火）；镇灯是暖琥珀、4 秒一轮。新火暖，旧火冷。

| 文件 | 改动 |
|---|---|
| `Assets/Script/WorldTraceBinder.cs` | 新增 `ElderLamps` 公开只读列表 + `EnsureElderLamps`（拴既有地标：裂脸石旁/riverbank/center/highland_east 锚点环上确定性取点，种子 `Fnv1a("elderlamp|{key}")`；运行时地物同裂脸石口径，零存档）；`VoleLamp` 加 `elder` 标志；原在灯参数头（冷月白 (0.62,0.72,1.0)、HDR 1.6、intensity 0.7、range 3m、呼吸 ±14%/~10s） |
| `Assets/Script/VoleLampDriver.cs` | 同帧驱动两列表（ActiveVoleLamps+ElderLamps），`Drive` 按 `elder` 分支取参数——同一昼夜爬升/天气压暗纪律，两套灯效 |

- 验证：离线编译 0 错误（2026-08-13）。play 验证项：新档第一夜（无任何镇）四盏冷灯渐亮；
  镇成形后暖灯与冷灯同夜共存、色温可辨；快进镇散后冷灯照旧亮。


---

## V1 批次五（D9）：深层遗迹×4——地里沉着比你早得多的东西

### 为什么改（玩家视角）

- **地里沉着四件很老的东西**：石头区一块有画的石板、东边高地一圈缺口的立石、
  台地中央一簇崩口整齐的石片、河边一块中间凹下去的扁石。它们在你到来之前
  就长眠于此——平时完全看不见，连一点土色异样都没有。
- **它们偶尔会被翻出来**：一场大风暴的夜、或田鼠在附近打洞，都可能让其中一件
  重见天日。翻出来就一直在那里了，不会消失。
- **点到它们，世界只说"它什么样"，永不说"谁做的"**："石头被摆成这样，不会是
  自己的意思。摆的人没留下别的。"制造者没有名字，语料只给推断不给答案。
- **不在场的日子它们也可能出土**：缺席信会把这件事当作头等大事告诉你
  （与断枝同级），回来后可以去现场认一认。
- 设计拍板（2026-08-13，"韵而不案"）：遗物属于"更早的居住者/过客"，不命名、
  不描述形貌。田鼠不是制造者，是无意识的考古学家。岩棚画的赭石/骨白点描与
  天空岩画同源——视觉押韵，不写一行字解释。

### 改动清单

| 文件 | 改动 |
|---|---|
| `Assets/GlimmerDiary/Scripts/Data/WorldEntityData.cs` | `StratumRecord` 增 `relicKind` 字段（painting/stone_circle/tool_scatter/quern；旧档 null 兼容） |
| `Assets/GlimmerDiary/Scripts/Data/AnimalStateData.cs` | `WorldEventType` 增 `DeepRelicSurfaced`（payload=relicKind，targetId=所在区） |
| `Assets/GlimmerDiary/Scripts/Core/StratumSystem.cs` | 构造即预播种四件（沉底档/第 0 层/恒 witnessed=false，旧档同路径补播、幂等）；出露共用 D6 两钩子同表同概率；深层遗物出露发永久事件+编年史句（推断不给答案）；`Suspended` 挂起位——新世界 30 天预跑期间不掷遗物签（长眠不结束在玩家到达前），catch-up 缺席期间照掷；`LayerPhrase` 对 deeprelic 恒返回"世界诞生之初" |
| `Assets/GlimmerDiary/Scripts/WorldManager.cs` | 预跑段挂起/解挂 `_stratumSystem.Suspended`（同 EraSystem 口径） |
| `Assets/GlimmerDiary/Scripts/Core/AbsenceLetterComposer.cs` | `event_DeepRelicSurfaced` 注册 salience 3（永久事件档，缺席信必提） |
| `Assets/Script/TraceKit.cs` | 新增四个程序化网格：`Slab`（竖立五边形薄板）/`StandingStone`（拉长双锥立石）/`StoneFlake`（三角石片）/`QuernStone`（宽扁浅凹碟） |
| `Assets/Script/WorldTraceBinder.cs` | 地层渲染循环：未出露深层遗物完全跳过（不可见、不占每区渲染预算）；出露后 `SpawnDeepRelic` 恒久在地——岩棚画=半埋微倾石板+板面 9 点赭石/骨白点描（借天空岩画色板）、石环=5~7 立石围缺口的圆半埋参差、石器散布=6~9 石片贴地半埋、磨盘=凹面扁石凹面朝上；出露当日进留意清单（fresh 口径复用） |
| `Assets/Script/TraceCaptionBank.cs` | 新增 `deep_painting`/`deep_circle`/`deep_tools`/`deep_quern` 四组考古语料（各 1~2 条占位，设计者并行线扩写）+ `notice_deeprelic` 留意句；`Pick` 按 traceKey 后缀路由 relicKind 组，恒考古无 _mem 语域 |
| `Assets/Script/TraceCaptionUI.cs` | deeprelic 点击语也取地层上下文（只取断代句——{layer} 恒"世界诞生之初"；witnessed 恒 false） |
| `Assets/GlimmerDiary/Scripts/Editor/AnimalDriveSmokeTest.cs` | 新增 `RunDeepRelics` 10 断言（预播种/幂等/断代/挂起/出露/事件/编年史/恒久/语料/稳定选句）；`RunStrata` 既有断言改按 kind 取被测对象（预播种后 strata[0] 恒为遗物） |
| `Assets/Editor/GlimmerBuild.cs` | RunSmoke 注册 `DeepRelics`（24 项） |

### 所有权

| 世界字段 | 单写者 | 说明 |
|---|---|---|
| `StratumRecord.relicKind` / deeprelic 记录 | StratumSystem | 构造播种（幂等）+ 出露翻转，与既有 strata 同一写者 |
| `worldEvents` DeepRelicSurfaced | StratumSystem | append-only，永久事件档 |
| 深层遗物视觉 | WorldTraceBinder（L3 只读派生） | 零新 L2 状态 |

### 验证

- 离线编译：`dotnet build` 两程序集 0 错误（2026-08-13）。
- batchmode 冒烟 24 项 FAIL=1（仅既有已知 QCOrganic），DeepRelics 10/10 全绿；
  Strata/Exposure/Witnessed 等既有地层断言与预播种共存无恙。
- 测试设计注记：挂起/出露两日断言要求"quern 必中且 painting 同拍不中"
  （FindStormHitDate 带 miss 排除键）——同表同概率下邻居会在目标拍被顺手翻出，
  同 WingTraces 身份键教训。
- play 观测点：快进/推进一天按钮到风暴夜（E_env.A>0.7）后逐日看四区；
  出露当日日记边缘留意句（"{place}翻出了什么。很老，比这里的谁都老。"）；
  双击聚焦读小报考古语料；缺席数日回归，信里必提。
- 出包（BuildWindows）本轮叫停：使用者仍在编辑器内工作；另工作区有设计者
  未完成的田鼠灯（VoleLampDriver）半成品引用。待拍板后重跑。
