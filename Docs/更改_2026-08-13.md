# 更改记录 2026-08-13

## 批次三（V1 D5+D6）：新生地层——沉降、出露、记忆双读

### 为什么改（玩家视角）

- **旧痕迹不再凭空消失，而是沉进土里**：以前土堆太多时最老的会被"剪掉"。
  现在它会先塌成一小片旧迹（还能点，语气是"它们住过这里"），再慢慢沉成
  地表上一点几乎看不出的土色异样——存在过的东西只是沉下去了，没有被删掉。
  塌洞也从出生那天起就踏上这条路：永远留下，但会一年年往下沉。
- **沉下去的速度看世界怎么过日子**：风大的年份沙埋得快，雨多的年份泥盖得快，
  草盛的地方根缠得快。埋得深不深，本身就是那几年的记录。
- **沉底的东西偶尔会被翻出来**：风暴夜（激烈情绪之后）可能把旧物剥出地表；
  田鼠在附近打新洞，也可能把隔壁的旧东西翻上来。重见天日的那件会半埋在原地，
  当天会有一个注意标记指给你看——旧物回来了，这是新闻。
- **同一件旧迹，见证过它和没见证过的人，读到的不是同一句话**：
  它发生时你在场（在线看到，或后来读信知道了它），点它时说的是
  "这堆土你见过的——镇子还在的时候。如今沉下去半截了"；
  它属于你错过的年代，说的就是"一处旧迹。比你早"。
  每句都带着它入土时的年代感——那年代由世界翻过几页来定，不由时钟来定。

### 改动清单

| 文件 | 改动 |
|---|---|
| `Assets/GlimmerDiary/Scripts/Data/WorldEntityData.cs` | 新增 `StratumRecord`（sourceKey/kind/zone/buriedDateKey/chapterOrdinal/chapterAtBurial/depth/exposed 三件套/witnessed；`RelicMaxDepth=1.0`、`MaxPerZone=8` 常量）；`StateChangeRecord`/`PermanentTerrainRecord`/`VoleTrailRecord` 加 `witnessed` |
| `Assets/GlimmerDiary/Scripts/Data/WorldStateData.cs` | `WorldSaveData` 新增 `strata`（旧档 null 零迁移，StratumSystem 构造 ??= 兜底）；`WorldChronicleEntry` 新增 `witnessKeys`（信件点名的痕迹键） |
| `Assets/GlimmerDiary/Scripts/Data/AnimalStateData.cs` | `WorldEvent` 加 `witnessed`（旧档默认 false=考古语气，语义恰好正确） |
| `Assets/GlimmerDiary/Scripts/Data/TraceKeyUtil.cs` | `MoundKeepCount=6` 常量化（binder 裁剪与地层入土共用）；`MoundRecord` 扩 trigger/toValue/witnessed；新增 `EnumVisibleMounds`（最新 N 窗口唯一实现） |
| `Assets/GlimmerDiary/Scripts/Core/StratumSystem.cs` | **新增**：`strata` 单写者，每 SimulatePass 恰 Tick 一次（VoleTownSystem 之后）。①入土：土堆出窗/小径淡完/塌洞出生；②沉降：`depth += BaseRate×(1+风×1.2+雨×0.8+草×0.6)`，只增不减，出露即冻结；③出露：风暴夜（E_env.A>0.7）8%、田鼠同区打洞 15%，确定性掷签 `Fnv1a(键\|日期\|钩子)`；封闭层断代=回放 ChapterTurned 事件（零新增字段）；`LayerPhrase` 章节→断代句 |
| `Assets/GlimmerDiary/Scripts/WorldManager.cs` | SimulatePass 挂 `_stratumSystem.Tick`；WorldTick 非 catch-up 拍前后 `WitnessSnapshot`/`WitnessStampNew`（在场打戳，集中单点，未来新系统零接入）；`MarkWitnessed`（读信回执：塌洞源记录+地层记录同步翻真）；`TryGetStratumContext` 透传（L3 不导入 Core 纪律）；`CollectWindowCollapses` 出参补身份键 → Compose 传 `collapseWitnessKeys` |
| `Assets/GlimmerDiary/Scripts/Core/AbsenceLetterComposer.cs` | `Compose` 新增 `collapseWitnessKeys` → 落条目 `witnessKeys` |
| `Assets/Script/ChronicleLetter.cs` | 读信时 `WorldManager.MarkWitnessed(save, e.witnessKeys)`（展示层簿记，经 WorldManager 静态入口写入） |
| `Assets/Script/WorldTraceBinder.cs` | T1 改走 `EnumVisibleMounds` 单一来源（种子保持去前缀键，土堆位置零漂移）；T2 塌洞移交地层分支（防同拍双画）；新增 strata 渲染三态：遗存（塌矮/色沉/微陷，可点）、地层（depth≥RelicMaxDepth 未出露：一点土色异样，**不可点**，每区预算 MaxPerZone=8，记录永不删）、出露（半埋挺回，可点，当日上新鲜标记）；新类型 `Relic`/`ExposedRelic`；签名纳入 strata 计数与 exposed 标志；顺带清理死代码（birthDays 字典与 TraceInstance.birthDay 字段，只写不读） |
| `Assets/Script/TraceCaptionBank.cs` | 新增 `relic_mem`/`relic_arch`/`exposed_mem`/`exposed_arch` 四语料组（占位各 1~2 条，设计者并行线扩写）；`Pick` 增加 `witnessed`/`layerPhrase` 参数：relic/exposed 按见证标记选语域，`{layer}` 占位统一替换（缺省回退"很久以前"） |
| `Assets/Script/TraceCaptionUI.cs` | relic/exposed 点击语经 `WorldManager.TryGetStratumContext` 取 witnessed 档+断代句注入 Pick |
| `Assets/GlimmerDiary/Scripts/Editor/AnimalDriveSmokeTest.cs` | BuildPipeline 挂 `StratumSystem`（镜像 WorldManager 序）并暴露 env；新增 `RunStrata`（出窗入土/沉降累计/过档/塌洞出生即入土+断代锚/翻页后封闭层 5 断言）、`RunExposure`（风暴必中必不中/深度门/田鼠翻土区域门/出露冻结 6 断言，掷签测试内自算期望值）、`RunWitnessed`（打戳只戳新生/读信回执双翻真/witnessKeys 落信/双语域/占位回退 5 断言） |
| `Assets/Editor/GlimmerBuild.cs` | RunSmoke 注册 Strata、Exposure、Witnessed（20 项） |
| `Assembly-CSharp.csproj` | 手补 StratumSystem.cs（离线编译用） |

### 所有权表新增

| 字段 | 写者 | 读者 |
|---|---|---|
| `WorldSaveData.strata` | StratumSystem（唯一） | WorldTraceBinder（派生渲染）、WorldManager.MarkWitnessed（witnessed 翻真）、测试 |
| 各记录 `witnessed` | WorldManager（WitnessStampNew 在场打戳 / MarkWitnessed 读信回执，唯二入口） | TraceCaptionUI（经 TryGetStratumContext 选语域）、TraceKeyUtil.MoundRecord（入土继承） |
| `WorldChronicleEntry.witnessKeys` | AbsenceLetterComposer（成信时落） | ChronicleLetter（读信时回放给 MarkWitnessed） |
| `{layer}` 文本占位 | StratumSystem.LayerPhrase（章节→断代句唯一判定） | TraceCaptionBank.Pick（经 TraceCaptionUI←WorldManager 透传） |

### 设计红线复核

- 记录只增不删（不可逆原则③）：渲染预算阀只影响画不画，不影响记录在不在。
- 出露掷签确定性（Fnv1a 哈希，不用 Random）：同一存档同一天同一结果，重启/回放可复现。
- witnessed 不暴露任何数值 UI——它只改变一句话的说法（验收三问的"误读空间"保留：
  读信知道了 ≠ 真的在场，但信这么说了，就算数）。
- 塌洞方案 A（2026-08-12 拍板）：出生即入土缓慢沉降，"永久"体现在记录永不删。

### 验证

- 离线编译：`dotnet build` Assembly-CSharp + Assembly-CSharp-Editor 均 0 错误（2026-08-13）。
- batchmode 冒烟 20 项（smoke_b3.log）：断言 FAIL=1，即既有已知 QCOrganic，无回归。
  - Strata 5 断言全绿：第 7 堆出窗入土/10 天沉降 0.07→0.74/过 RelicMaxDepth 入地层档/塌洞出生即入土+断代锚荒年/翻页后封闭层第 1 层+"雨季正盛的时候"。
  - Exposure 6 断言全绿：风暴必中签出露/必不中按兵不动/深度门（未沉底无资格）/田鼠翻土同区出露/区域门（隔壁区不动）/出露后深度冻结。
  - Witnessed 5 断言全绿：打戳只戳本拍新生/读信回执源记录+地层记录同步翻真/witnessKeys 随信落档/双语域分句+断代注入/缺省回退不留占位符。
- 首轮冒烟抓到两处实施缺陷，均为本批新增代码自身问题，已修：
  ① `TraceCaptionBank` 新语料组收尾符误写成 `],`（数组初始化误闭字典项），编译期即拦下；
  ② Witnessed 测试给 Compose 喂了 salience 0 的噪音条目导致不成信、断言空转——
  换成永久事件档条目后转绿（同批次二教训：断言要对准设计语义，不只对准 API）。

---

## 点击失灵根因修复（批次二挂号项，D9 前置清除）

### 为什么改（玩家视角）

- **点世界的点击全部恢复了**：点草地草会簌动，点痕迹/亮斑会推近出观察语。
  之前点哪都没反应，不是点击坏了，是"别点穿 UI"的保护闸门认错了人——
  它把整片草原当成了一块 UI 面板，替你"挡住"了每一次点击。

### 根因（实证链）

1. 主相机上挂着一个场景模板遗迹 `PhysicsRaycaster`（PlantGrowthTemplate.unity:12114，
   事件掩码=全层），全项目无任何消费者——但没有 EventSystem 时它完全无害，躺了很多周。
2. 08-03 demo 收口（768ba20）运行时自建 EventSystem + InputSystemUIInputModule（日记 UI 需要），
   `TraceInput` 的闸门① `IsPointerOverGameObject()` 从此刻起才真正生效
   （此前 `EventSystem.current == null` 恒短路 false——这就是"之前一直正常"的原因；
   08-03~08-12 三批验证全走键盘快进+批处理冒烟，无人点击，故迟至 08-12 暴露）。
3. 新输入模块把"指针射线打到的第一个物体"直接记为 `pointerEnter`（InputSystemUIInputModule.cs:528），
   **不过滤是否 UI**；`IsPointerOverGameObject()` 只查 `pointerEnter != null`（同文件:322）。
   PhysicsRaycaster mask=全层 → 悬停地形/痕迹恒有命中 → 闸门①吞掉一切世界点击。
4. 唯一能过闸门的点击是点天空（无碰撞体）→ 随后真的什么都没打到，
   报"射线未命中任何碰撞体"。两个症状，一个病。

### 改动清单

| 文件 | 改动 |
|---|---|
| `Assets/Script/TraceInput.cs` | 闸门①改为 `PointerOverUGUI`：`EventSystem.RaycastAll` 后只认 `GraphicRaycaster` 命中（当前项目无任何 GraphicRaycaster → 恒 false，信/日记防穿透由闸门② IsOpen 兜底；未来接入可点击 uGUI 时自动生效）。排查日志按 9194b03 约定全部清除 |

### 验证

- 离线编译 0 错误；play 实测（2026-08-13 用户确认）：点草簌动 ✓、点痕迹/亮斑推近+语料 ✓、
  信开着点世界不穿透、关信恢复 ✓。

### 遗留雷（已挂号，未动）

- 主相机 PhysicsRaycaster 仍在场景里（mask=全层）。本次修复后它不再影响点击，
  但任何未来的 `IsPointerOverGameObject` 调用都会踩同一个坑——建议择机在 Inspector 移除并存场景
  （场景手工组件删除，未由 Claude 代劳）。

