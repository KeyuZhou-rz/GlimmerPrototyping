# 更改 2026-07-29

（承接 07-28 的 wave1 批次：T7 压痕 / Accept 验收工具 / 痕迹语料见 `更改_2026-07-28.md`）

# wave1 · C1 T1 水毁跨 session 锁存（湿度极值）

## 为什么改

洪水泡透的土堆会塌成湿泥——这是"不可逆"的承诺（宪法③）。但之前有个漏洞：
如果你在水毁发生后、湿度自然回落的间隙离开游戏，下次回来时土堆会悄悄"复原"成完好形态。
原因是锁存判定看的是"现在还湿不湿"，而不是"曾经湿没湿过"。
本批给每个地区记一个**历史湿度极值**（只增不减），水毁改判极值——
水退可以，但"这里曾经泡透过"这件事，世界会一直记得。

## 玩家会看到什么

被水泡塌的土堆，永远保持塌掉的形态。不会再出现"过两天回来看，它又鼓起来了"。

## 改动清单

| 文件 | 改动 |
|---|---|
| `Assets/GlimmerDiary/Scripts/Data/WorldEntityData.cs` | `LocationEntity` 新增 `soilMoisturePeak`（历史湿度极值，只增不减；JsonUtility 天然向后兼容旧档读出 0） |
| `Assets/GlimmerDiary/Scripts/WorldManager.cs` | ① `PropagateRainfallToLocations` 内维护极值（单写者内部）；② 加载后旧档迁移：`peak = max(peak, soilMoisture)` 兜底 |
| `Assets/GlimmerDiary/Scripts/Utils/EntityStateHelper.cs` | 规则路径写 soilMoisture 时同步抬极值 |
| `Assets/GlimmerDiary/Scripts/Core/WorldInitializer.cs` | 新世界极值锚定初始湿度（从出生当天记起） |
| `Assets/Script/WorldTraceBinder.cs` | `IsFloodDamaged` 路径①改判 `soilMoisture > 阈值 \|\| soilMoisturePeak > 阈值`；路径②（洪水搬家历史）保留。重建签名无需加 peak——极值变化必伴随湿度量化档变化 |
| `Assets/GlimmerDiary/Scripts/Utils/WorldSimulationTester.cs` | 新增场景十一 `FloodLatchPeak`：12 天暴雨冲线 → 20 天晴天回落，五断言 |

## 所有权

| 数据或输出 | 单一写入者 | 下游读取者 | 本轮变化 |
|---|---|---|---|
| `loc.soilMoisturePeak` | `WorldManager.PropagateRainfallToLocations`（主）；`EntityStateHelper.ChangeLocationState`（规则路径同步）；`WorldInitializer`/加载迁移（锚定） | `WorldTraceBinder.IsFloodDamaged` | 新增字段；与 soilMoisture 同写者、同序列化通道 |

## 验证

| 验证项 | 结果 |
|---|---|
| 场景十一（play） | 五断言全绿：湿度冲过 0.8（实际 1.00）→ 晴 20 天回落 0.62；极值保持 1.00 且只增不减 ✅ |
| 编译 | 0 error |
| 待玩家验收 | 完整跨 session 复现（水毁土堆在湿度回落后重进 play 仍保持塌陷）——数据条件已由场景十一覆盖，视觉端留 playtest 确认 |

---

# wave1 · 文档收口（D2 合并删旧 + 全景调查拍板回填）

## 为什么改

07-28 全景调查后逐条拍板了一批"做/不做/暂缓"。这些决定如果只留在对话里，
下个季度翻设计文档时就会看到一堆"待设计"的幽灵行——文档必须与拍板同步，
否则计划会悄悄漂离实际造出来的东西。同时两份并存的工作台（旧
`InteractionWorksheet.md` 与矩阵补全）合并为一份，从此只有一个"游戏的根"。

## 拍板回填清单（2026-07-28 定稿，已写入矩阵补全 §0 决策表）

| 项 | 结论 |
|---|---|
| B1 季节色 | **验收通过**——Tools 验证已见差别，夏色已调为夏冬均值；旱枯黄档留后续 playtest |
| A 组树视觉批 | **暂缓**——树的方案未定；树荫微气候等明显超出当前完成度，随树方案一并重启 |
| C2 石边草 | **由草色通路兑现，实体不再单做**——stone_area 枯黄/转绿走 GrassPreset 统一映射 |
| C3 绒苗限期 | **维持 15 天撤出**，不延长 |
| C4 多风向 | **不做** |
| C5 水面反射 | **撤出**——河面离画面太远，投入产出比低；反日侧远水偏青一并挂起 |
| C6 信形态 | **维持占位**（ChronicleLetter L 键），设计稿换皮另议 |
| C7 即时情绪应答 | 方向暂定**远音**；音轨未做，本批不落实 |
| C8 动物 3D | **永久封存**——玩家只见痕迹不见动物，与宪法⑤一致 |
| C9 候鸟在场正效应 | **正式废弃** |
| D1 翻译层 Track B（有状态信号 4/5/6 迁移） | **不迁，轨道关闭**——所有权表 6/6 已于 07-20 收口 |

## D2 合并删旧

旧 `Docs/InteractionWorksheet.md` 逐行 diff 核对：全文已被矩阵补全覆盖，仅三处
独有内容——GameplayLab 协议段（§0）、T3 脚印"前 3 日各自压草"细节、T7 旧口径
（已按 wave1 定稿改写）——并入矩阵补全后删除。矩阵补全头部加"唯一工作台"声明。

## 改动清单

| 文件 | 改动 |
|---|---|
| `Docs/InteractionWorksheet.md` | **删除**（内容全部并入矩阵补全） |
| `Docs/交互设计工作台_2026-07-21_矩阵补全.md` | §0 新增 07-28 拍板决策表 + 补入 GameplayLab 协议段；T3 行补脚印压草细节；T7 行更新为 wave1 定稿（色片+半径 5.5+舞台机位验收）；石边草行/候鸟正效应行/草色三合一行/树荫行/蒲公英限期/信形态逐项打拍板标注；头部加唯一工作台声明 |
| `CLAUDE.md` | 三处 InteractionWorksheet.md 引用改指矩阵补全 |
| `AGENTS.md` | 同上（OpenCode 镜像规则） |
| `Docs/ProjectSummary.md` | Docs 目录清单与文末署名引用改指矩阵补全 |
| `Docs/AnimalStateSystem.md` | P4 fox(Hunger) 文案欠账勾销（已随 07-28 语料批补上） |

## 验证

| 验证项 | 结果 |
|---|---|
| 合并完整性 | `git diff` 逐行核对旧文档 vs 矩阵补全：除上述三处独有内容（已并入）外零丢失 ✅ |
| 引用清扫 | 全仓 grep `InteractionWorksheet`：活文档零残留，历史 changelog 中的引用按惯例保留 ✅ |
