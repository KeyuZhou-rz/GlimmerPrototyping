# 更改 2026-07-26：Gameplay Lab 实验协议定稿

## 为什么改

联网和正式日记界面还没有接入，但这不应阻止我们先判断世界是否值得观察。新的实验协议让研究者在幕后手动输入外部五维，每次只推进一天；玩家只面对草原、天气和痕迹，不会看到情绪数值或快进按钮。

协议最重要的作用是把“输入当天的响应”和“世界经过一天后的自主变化”分开。这样后续看到脚印、积水或动物留下的痕迹时，可以判断它属于哪一个阶段，而不是把两次模拟混成一次即时反馈。

## 改动清单

| 文件 | 改动 |
|---|---|
| `Docs/GameplayLabLrotocol.md` | 新增手动五维实验的权威协议；包含数据提案 `20260726-01`、控制器状态机、外部样本交接、Submit/Tick 时序、保存规则、Debug Log v1 schema、错误码和实现验收 |
| `Docs/InteractionWorksheet.md` | 在已拍板决策处加入 Gameplay Lab 协议入口，明确后续实验与开发以该文档为准 |

## 所有权

| 数据/动作 | 单一写者 | 读取者 |
|---|---|---|
| 外部样本记录 | 研究者的外部实验记录 | 研究者手动录入 |
| Inspector 待提交五维（提案字段） | 研究者 | 未来 `ManualGameplayLab` |
| `E_env` | `EmotionInertiaSystem` | L2、L3、未来 Debug Snapshot |
| `gameTime` | `WorldManager.WorldTick` | 世界系统、未来 Debug Snapshot |
| Gameplay Lab Debug Log | 未来 `ManualGameplayLab` | 研究者 |

没有新增生产数据、存档字段或世界写者。

## 验证

1. 对照 `WorldManager.OnJournalSubmitted()`：协议记录了 catch-up → 注入 → 当前日响应式模拟 → 世界/日记存盘。
2. 对照 `WorldManager.WorldTick(1)`：协议记录了日期 +1 → `E_env` 以 0.05 回落 → 自主模拟，并明确 Tick 后需由控制器补存盘。
3. 对照 `EmotionInertiaSystem`：Submit 的 alpha 固定沿用 `lerp(0.1, 0.3, C)`，自主日不重复读取外部向量。
4. 对照 `SaveSystem`：未提议任何 JSON 格式变化，Debug Log 不持久化到世界存档。
5. 本批仅文档，无 C# 或场景修改；未执行编译和 Play 验证。

## 审批状态

数据提案 `20260726-01` 仍为待审批。未获得设计者明确批准前，不实现 `ManualGameplayLab`，不修改实验场景。
